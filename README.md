# Список исправленных ошибок в соответствии со стандартом Epic Games C++ Coding Style
## Вариант 1
### Task 1:
1. Глобальные директивы
  - Убрано using namespace std в глобальной области видимости, добавлен префикс std:: ко всем объектам стандартной библиотеки
2. Именование функций
  - Функции переименованы в PascalCase (с заглавной буквы)
  - Убраны нижние подчёркивания из имён функций
3. Именование параметров функций
  - Параметры переименованы в PascalCase (с заглавной буквы)
  - Имена параметров сделаны информативными (отражают их назначение)
  - Пример: Mvector → Matrix, i → Row, j → Col, Wvector → Weights
4. Именование переменных в main()
  - Все переменные переименованы в PascalCase (с заглавной буквы)
  - Пример: weight_vector → Weights, main_vector → WeightMatrix
  - К имени логических переменных добавлен префикс 'b': flag → bFlag
5. Инициализация переменных
  - Все переменные инициализированы при объявлении (присвоены начальные значения)
6. Форматирование кода
  - Добавлены фигурные скобки для всех управляющих конструкций (if, for, while) даже с одной строкой
  - Фигурные скобки открываются в новой строке
  - Добавлены пробелы вокруг бинарных операторов ('+', '-', '*', '/', '=')
  - Убраны лишние пробелы в угловых скобках: vector <vector <int>> → std::vector<std::vector<int>>
7. Инкремент/декремент
  - Заменён постфиксный инкремент/декремент на префиксный
8. Условные операторы
  - Убраны избыточные сравнения с true/false
9. Структура проекта
  - Программа разделена на модули согласно стандарту Epic Games
  - Создан заголовочный файл Task1.h с прототипом функции
  - Реализация функции вынесена в отдельный файл Task1cpp.cpp
  - В основном файле Task1.cpp оставлена только функция main

### Task 2:
1. Глобальные директивы
  - Убрано using namespace std в глобальной области видимости, добавлен префикс std:: ко всем объектам стандартной библиотеки
2. Именование переменных 
  - Все переменные переименованы в PascalCase (с заглавной буквы)
  - Пример: n → N, route → Route, inputFile → InputFile
3. Инициализация переменных
  - Все переменные инициализированы при объявлении (присвоены начальные значения)
4. Форматирование кода
  - Фигурные скобки открываются в новой строке
  - Убраны лишние пробелы в угловых скобках: vector <vector <int>> → std::vector<std::vector<int>>
5. Комментарии
  - Комментарии исправлены: начинаются с заглавной буквы
  - Добавлен пробел после // в комментариях
6. Инкремент/декремент
  - Заменён постфиксный инкремент/декремент на префиксный
  - Примеры: row-- → --Row, col++ → ++Col
7. Локализация
  - Добавлена поддержка русского языка

### Task 3:
1. Глобальные директивы
  - Убрано using namespace std в глобальной области видимости, добавлен префикс std:: ко всем объектам стандартной библиотеки
2. Именование переменных 
  - Все переменные переименованы в PascalCase (с заглавной буквы) и сделаны информативными
  - Пример: k → K, tz → NumbersWithTwoZeros, _oz → PrevNumbersEndingWithZero
3. Инициализация переменных
  - Все переменные инициализированы при объявлении (присвоены начальные значения)
  - Пример: int K = 0, N = 0;
4. Форматирование кода
  - Фигурные скобки открываются в новой строке
  - Добавлены пробелы вокруг операторов сравнения: 2<=K<=10 → 2 <= K <= 10
5. Инкремент/декремент
  - Заменён постфиксный инкремент на префиксный: i++ → ++i
6. Локализация
  - Добавлена поддержка русского языка

### Task 4:
1. Глобальные директивы
  - Убрано using namespace std в глобальной области видимости, добавлен префикс std:: ко всем объектам стандартной библиотеки
2. Именование функций
  - Функции переименованы в PascalCase (с заглавной буквы)
  - Убраны нижние подчёркивания из имён функций
3. Именование параметров функций
  - Параметры переименованы в PascalCase (с заглавной буквы)
  - Имена параметров сделаны информативными (отражают их назначение)
  - Пример: s → Expression, Text, Expression
  - Параметры передаются как константные ссылки (const std::string&) для избегания лишнего копирования
4. Именование локальных переменных
  - Все локальные переменные переименованы в PascalCase (с заглавной буквы) и сделаны информативными:
  - Пример: i → Index, level → Level, pos → OperatorPosition, op → Operator
5. Прототипы функций
  - Исправлен прототип функции CalculateExpression перед её использованием
6. Инициализация переменных
  - Все переменные инициализированы при объявлении (присвоены начальные значения)
7. Форматирование кода
  - Фигурные скобки открываются в новой строке
8. Инкремент/декремент
  - Заменён постфиксный инкремент/декремент на префиксный: level++ → ++Level, level-- → --Level, i-- → --Index
9. Структура проекта
  - Программа разделена на модули согласно стандарту Epic Games
  - Создан заголовочный файл Task4.h с прототипом функции
  - Реализация функции вынесена в отдельный файл Task4cpp.cpp
  - В основном файле Task4.cpp оставлена только функция main
